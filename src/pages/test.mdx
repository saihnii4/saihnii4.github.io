---
title: Hello, world!
slug: /test
date: 2019-01-29
---

# Heading (rank 1)
## Heading 2
### Heading 3
#### Heading 4

> Block quote

* Unordered
* List

1. Ordered
2. List

A paragraph, introducing a thematic break:

---

```rust
#[derive(Main)]
struct Test;

impl TypeMapKey for Test {
    type Value = Arc<RwLock<HashMap<i32, i32>>>;
}
```

a [link](https://example.com), an  some *emphasis*,
something **strong**, and finally a little `code()`.
```rust
#[derive(Main)]
struct Test;

impl TypeMapKey for Test {
    type Value = Arc<RwLock<HashMap<i32, i32>>>;
}
```

a [link](https://example.com), an  some *emphasis*,
something **strong**, and finally a little `code()`.
```rust
#[derive(Main)]
struct Test;

impl TypeMapKey for Test {
    type Value = Arc<RwLock<HashMap<i32, i32>>>;
}
```

a [link](https://example.com), an  some *emphasis*,
something **strong**, and finally a little `code()`.
```rust
#[derive(Main)]
struct Test;

impl TypeMapKey for Test {
    type Value = Arc<RwLock<HashMap<i32, i32>>>;
}
```

a [link](https://example.com), an  some *emphasis*,
something **strong**, and finally a little `code()`.
```rust
#[derive(Main)]
struct Test;

impl TypeMapKey for Test {
    type Value = Arc<RwLock<HashMap<i32, i32>>>;
}
```

a [link](https://example.com), an  some *emphasis*,
something **strong**, and finally a little `code()`.